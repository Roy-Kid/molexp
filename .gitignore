
__pycache__/
__pypackages__/
!src/molexp/dist/.gitkeep
.agent_provider.json
.cache
.claude/scheduled_tasks.lock
.claude/specs/
.claude/worktrees/
.coverage
.coverage.*
.dmypy.json
.DS_*
.eggs/
.env
.hypothesis/
.installed.cfg
.ipynb_checkpoints
.mcp_secrets.json
.mypy_cache/
.nox/
.pdm.toml
.playwright-mcp/
.pybuilder/
.pyre/
.pytest_cache/
.Python
.pytype/
.ropeproject
.ruff_cache/
.scrapy
.spyderproject
.spyproject
.tox/
.venv
.webassets-cache
*.cover
*.egg
*.egg-info/
*.log
*.manifest
*.mo
*.pot
*.py,cover
*.py[cod]
*.sage.py
*.so
*.spec
*.sqlite-shm
*.sqlite-wal
*.sublime*
**/*.back.*
**/*.backup.*
*$py.class
/agent/
/cache/
/catalog/
/e2e_run.py
/index.json
/INDEX.md
/lib/
/lib64/
/meta.yaml
/project.json
/projects/
/site
/workspace.events.sqlite
/workspace.json
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   commonly ignored for libraries.
#   For a library or package, you might want to ignore these files since the code is
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   https://pdm.fming.dev/#use-with-ide
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#   in version control.
#   install all needed dependencies.
#   intended to run in multiple environments; otherwise, check them in:
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#  Usually these files are written by a python script from a template
# .python-version
# (running `molexp run` from the repo root writes a whole workspace here)
# Accidental agent/session materialisation at repo root (never commit)
# ad-hoc e2e runner left at repo root
# Agent home: agent/ (sessions + _tasks + .scratch).
# Byte-compiled / optimized / DLL files
# C extensions
# Celery stuff
# Claude Code workflow scratch — in-flight specs, runtime artifacts, worktrees
# Compiled frontend bundle (populated by `npm run build` in ui/)
# Cython debug symbols
# Distribution / packaging
# Django stuff:
# Environments
# Flask stuff:
# Installer logs
# IPython
# Jupyter Notebook
# LSP config files
# mkdocs documentation
# molexp workspace artifacts accidentally materialised at the repo root
# mypy
# pdm
# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
# pipenv
# poetry
# Poetry local configuration file - https://python-poetry.org/docs/configuration/#local-configuration
# PyBuilder
# PyCharm
# pyenv
# PyInstaller
# Pyre type checker
# pytest --basetemp=tmp/ leftovers (tmp_path fixture output landing in repo root)
# pytype static type analyzer
# Rope project settings
# ruff
# SageMath parsed files
# Scrapy stuff:
# Scratch workspaces materialised by CLI examples
# Sphinx documentation
# Spyder project settings
# SQLite WAL/SHM sidecars (transient — never commit)
# Translations
# Unit test / coverage reports
# Workspace-level agent secrets — never commit
#.idea/
### Python ###
### Python Patch ###
### react ###
#pdm.lock
#Pipfile.lock
#poetry.lock
bower_components
build/
celerybeat-schedule
celerybeat.pid
cover/
coverage.xml
cython_debug/
db.sqlite3
db.sqlite3-journal
develop-eggs/
dist/
dmypy.json
docs/_build/
downloads/
eggs/
env.bak/
env/
ENV/
examples/**/_workspace/
examples/agent/smoke_runs/
htmlcov/
instance/
ipython_config.py
local_settings.py
logs
MANIFEST
node_modules
nosetests.xml
openapi.json
package-lock.json
parts/
pip-delete-this-directory.txt
pip-log.txt
poetry.toml
profile_default/
psd
pyrightconfig.json
scripts/live/.out/
scripts/live/pe_rg_e2e/.run/
sdist/
share/python-wheels/
sketch
src/molexp/dist/*
target/
thumb
tmp/
uv.lock
var/
venv.bak/
venv/
wheels/
