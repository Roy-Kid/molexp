"""Native tool collection (spec §3, §6.2).

Phase 2 lands the actual tool implementations
(:mod:`.workspace`, :mod:`.workflow`, :mod:`.chat`). Until then this
package is intentionally empty so the import-guard test sees the
target layout from day one.
"""

__all__: list[str] = []
