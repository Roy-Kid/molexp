/**
 * @deprecated Use {@link RunToolbar} — single status-gated header for RunViewer
 * and MolqRunViewer. Kept as a thin re-export so any residual import still works.
 */

export { RunToolbar as RunActions, type RunToolbarProps as RunActionsProps } from "./RunToolbar";
